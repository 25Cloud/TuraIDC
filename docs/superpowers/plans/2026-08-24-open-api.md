# 开放 API（Open API v2）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 TuraIDC 提供一套基于 API Key 的开放接口（`/api/v2/open/*`），支持系统间对接/转售、无限层级复合对接，含用户端密钥管理与管理端配置/审计。

**Architecture:** 独立 open 网关层，`api.key` 中间件负责密钥认证 + scope 校验 + IP 白名单 + 调用审计；Open 控制器保持薄层，全部复用现有 Service（CheckoutService、ServicePowerService、ServiceRenewService、ProductCatalogService）。密钥明文仅创建时返回一次，数据库只存哈希。管理端配置走现有 `SettingService`（group=`open_api`）。

**Tech Stack:** PHP 8.3 / Laravel 12 / Sanctum；Vue 3 + TDesign（frontend-user-v4-console、frontend-admin-v3）。

**前置阅读（工程师必读）：**

- 设计文档：`docs/superpowers/specs/2026-08-24-open-api-design.md`
- 现有 API 格式约定：`docs/references/api/api-format.md`（`{code, message, data, timestamp}`，错误码 40100/40300/42200/50000）
- 中间件参考：`backend/app/Http/Middleware/EnsureClientAuthenticated.php`
- 下单参考：`backend/app/Console/Commands/LocalProductSimulateCommand.php`（`doNew()` 展示 quote→create→payByBalance 完整链路）
- 路由注册参考：`backend/routes/v2-client.php`（业务路由）、`backend/routes/api.php` 或 `bootstrap/app.php`（确认中间件别名注册处）
- 用户端路由参考：`frontend-user-v4-console/src/router/modules/client.ts`
- 管理端菜单参考：`frontend-admin-v3/src/store/modules/permission.ts`（`ADMIN_MENU_GROUPS`）、设置页参考 `frontend-admin-v3/src/pages/settings/index.vue`（`settingsTab` meta 模式）

**关键 Service 签名（实现时直接调用）：**

- `CheckoutService::quote(Product $product, string $billingCycle, array $config, int $quantity): array`（返回 total_amount 等）
- `CheckoutSecurityService::issueQuoteToken(int $productId, string $billingCycle, array $config, array $quotePayload): array`（返回 `['quote_token' => ...]`）
- `CheckoutService::create(int $userId, array $data, array $context): Invoice`（`$data`: product_id/billing_cycle/quantity/config/quote_token/user_coupon_id；`$context`: idempotency_key/trace_id/ip_address）
- `PaymentService::payByBalance(Invoice $invoice, User $user, array $context): array`（`$context`: trace_id）
- `ServicePowerService::powerActionForUser(User $user, int $serviceId, string $action, array $context = []): array`
- `ServiceRenewService::createRenewInvoiceForUser(User $user, int $serviceId, string $billingCycle, int $couponId, array $context): Invoice`
- `ServiceConsoleController::renewPreview(...)` / `ServiceDetailService` 服务详情

---

### Task 1: 数据库迁移（api_keys / api_key_usage_logs）

**Files:**

- Create: `backend/database/migrations/2026_08_24_000001_create_api_keys_table.php`
- Create: `backend/database/migrations/2026_08_24_000002_create_api_key_usage_logs_table.php`

- [ ] **Step 1: 编写 api_keys 迁移**

```php
<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('api_keys', function (Blueprint $table) {
            $table->bigIncrements('id');
            $table->unsignedBigInteger('user_id');
            $table->string('name', 64)->default('');
            $table->string('key_prefix', 32)->unique();
            $table->string('secret_hash', 64);
            $table->string('secret_last4', 4);
            $table->json('scopes')->nullable();
            $table->timestamp('expires_at')->nullable();
            $table->json('ip_allowlist')->nullable();
            $table->string('status', 16)->default('enabled');
            $table->timestamp('last_used_at')->nullable();
            $table->timestamps();
            $table->softDeletes();

            $table->index('user_id');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('api_keys');
    }
};
```

- [ ] **Step 2: 编写 api_key_usage_logs 迁移**

```php
<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('api_key_usage_logs', function (Blueprint $table) {
            $table->bigIncrements('id');
            $table->unsignedBigInteger('api_key_id')->index();
            $table->unsignedBigInteger('user_id');
            $table->string('method', 8)->default('');
            $table->string('path', 255)->default('');
            $table->unsignedSmallInteger('status_code')->default(0);
            $table->string('ip', 45)->default('');
            $table->unsignedInteger('duration_ms')->default(0);
            $table->timestamp('created_at')->index();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('api_key_usage_logs');
    }
};
```

- [ ] **Step 3: 运行迁移**

Run: `cd backend && php artisan migrate`
Expected: 两张表创建成功，无报错。

- [ ] **Step 4: 提交**

```bash
git add backend/database/migrations/2026_08_24_000001_create_api_keys_table.php backend/database/migrations/2026_08_24_000002_create_api_key_usage_logs_table.php
git commit -m "feat: open api 密钥与调用审计表迁移"
```

---

### Task 2: ApiKey 模型 + ApiKeyService

**Files:**

- Create: `backend/app/Models/ApiKey.php`
- Create: `backend/app/Services/OpenApi/ApiKeyService.php`
- Create: `backend/app/Services/OpenApi/OpenApiConfig.php`
- Create: `backend/app/Services/OpenApi/ApiKeyUsageLogService.php`

- [ ] **Step 1: 编写 ApiKey 模型**

```php
<?php

declare(strict_types=1);

namespace App\Models;

use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\SoftDeletes;

class ApiKey extends Model
{
    use SoftDeletes;

    public const STATUS_ENABLED = 'enabled';

    public const STATUS_DISABLED = 'disabled';

    /** 密钥前缀（展示用） */
    public const KEY_PREFIX = 'tura_';

    protected $fillable = [
        'user_id', 'name', 'key_prefix', 'secret_hash', 'secret_last4',
        'scopes', 'expires_at', 'ip_allowlist', 'status', 'last_used_at',
    ];

    protected $casts = [
        'scopes' => 'array',
        'ip_allowlist' => 'array',
        'expires_at' => 'datetime',
        'last_used_at' => 'datetime',
    ];

    public function scopeEnabled(Builder $query): Builder
    {
        return $query->where('status', self::STATUS_ENABLED);
    }

    public function isExpired(): bool
    {
        return $this->expires_at !== null && $this->expires_at->isPast();
    }
}
```

- [ ] **Step 2: 编写 OpenApiConfig（读取 settings 组 open_api）**

```php
<?php

declare(strict_types=1);

namespace App\Services\OpenApi;

use App\Models\Setting;

class OpenApiConfig
{
    public const GROUP = 'open_api';

    public function enabled(): bool
    {
        return (int) Setting::getValue(self::GROUP, 'enabled', 0) === 1;
    }

    public function requirePhone(): bool
    {
        return (int) Setting::getValue(self::GROUP, 'require_phone', 0) === 1;
    }

    public function requireVerified(): bool
    {
        return (int) Setting::getValue(self::GROUP, 'require_verified', 0) === 1;
    }

    public function maxKeysPerUser(): int
    {
        return max((int) Setting::getValue(self::GROUP, 'max_keys_per_user', 10), 1);
    }

    public function rateLimitPerMinute(): int
    {
        return max((int) Setting::getValue(self::GROUP, 'rate_limit', 60), 1);
    }
}
```

- [ ] **Step 3: 编写 ApiKeyService**

```php
<?php

declare(strict_types=1);

namespace App\Services\OpenApi;

use App\Exceptions\BusinessException;
use App\Models\ApiKey;
use App\Models\User;
use Illuminate\Support\Str;

class ApiKeyService
{
    /** 业务域 => 支持级别（write 蕴含 read） */
    public const SCOPE_DOMAINS = ['products', 'orders', 'services', 'finance'];

    public function __construct(
        private readonly OpenApiConfig $config,
    ) {}

    /** 生成完整密钥（明文仅此一次），返回 [model, plainSecret] */
    public function createForUser(
        User $user,
        string $name,
        array $scopes,
        ?string $expiresAt = null,
        array $ipAllowlist = [],
    ): array {
        $this->assertCanCreate($user);

        $plain = Str::random(32);
        $prefix = ApiKey::KEY_PREFIX.Str::lower(Str::random(8));

        $key = ApiKey::query()->create([
            'user_id' => (int) $user->id,
            'name' => $name,
            'key_prefix' => $prefix,
            'secret_hash' => $this->hashSecret($plain),
            'secret_last4' => Str::substr($plain, -4),
            'scopes' => $this->normalizeScopes($scopes),
            'expires_at' => $expiresAt ?: null,
            'ip_allowlist' => $ipAllowlist !== [] ? $ipAllowlist : null,
            'status' => ApiKey::STATUS_ENABLED,
        ]);

        return [$key, $plain];
    }

    public function assertCanCreate(User $user): void
    {
        if (! $this->config->enabled()) {
            throw new BusinessException('系统未开放 API 密钥功能', 40300);
        }
        if ($this->config->requirePhone() && trim((string) $user->phone) === '') {
            throw new BusinessException('请先绑定手机号后再创建 API 密钥', 40300);
        }
        if ($this->config->requireVerified() && ! $user->hasCompletedVerification()) {
            throw new BusinessException('请先完成实名认证后再创建 API 密钥', 40300);
        }

        $count = ApiKey::query()->where('user_id', (int) $user->id)->count();
        if ($count >= $this->config->maxKeysPerUser()) {
            throw new BusinessException('API 密钥数量已达上限', 42200);
        }
    }

    /** 校验密钥：返回 ApiKey 或抛异常 */
    public function resolve(string $secret): ApiKey
    {
        if ($secret === '') {
            throw new BusinessException('缺少 API 密钥', 40100);
        }

        // 先按前缀定位候选（前缀是展示值的一部分，需与存储一致）
        $candidate = ApiKey::query()
            ->where('key_prefix', 'like', 'tura_%')
            ->where('status', ApiKey::STATUS_ENABLED)
            ->get()
            ->first(fn (ApiKey $key) => hash_equals($key->secret_hash, $this->hashSecret($secret)));

        if (! $candidate) {
            throw new BusinessException('API 密钥无效或已被停用', 40100);
        }

        if ($candidate->isExpired()) {
            throw new BusinessException('API 密钥已过期', 40100);
        }

        return $candidate;
    }

    /** 校验 scope：write 蕴含 read */
    public function assertScope(ApiKey $key, string $domain, string $level): void
    {
        $scopes = is_array($key->scopes) ? $key->scopes : [];
        $granted = (string) ($scopes[$domain] ?? '');

        if ($granted === $level || ($level === 'read' && $granted === 'write')) {
            return;
        }
        throw new BusinessException("当前密钥缺少 {$domain}:{$level} 权限", 40300);
    }

    /** 校验 IP 白名单 */
    public function assertIpAllowed(ApiKey $key, string $ip): void
    {
        $allowlist = is_array($key->ip_allowlist) ? array_values(array_filter($key->ip_allowlist)) : [];
        if ($allowlist === [] || in_array($ip, $allowlist, true)) {
            return;
        }
        throw new BusinessException('当前 IP 不在密钥白名单内', 40300);
    }

    public function touchLastUsed(ApiKey $key): void
    {
        $key->forceFill(['last_used_at' => now()])->saveQuietly();
    }

    public function normalizeScopes(array $scopes): array
    {
        $normalized = [];
        foreach (self::SCOPE_DOMAINS as $domain) {
            $value = (string) ($scopes[$domain] ?? '');
            if (in_array($value, ['read', 'write'], true)) {
                $normalized[$domain] = $value;
            }
        }
        return $normalized;
    }

    public function hashSecret(string $secret): string
    {
        return hash('sha256', $secret.config('app.key'));
    }

    public function update(ApiKey $key, array $payload): ApiKey
    {
        $key->fill([
            'name' => (string) ($payload['name'] ?? $key->name),
            'scopes' => $this->normalizeScopes((array) ($payload['scopes'] ?? $key->scopes ?? [])),
            'expires_at' => ($payload['expires_at'] ?? null) ?: null,
            'ip_allowlist' => isset($payload['ip_allowlist']) && is_array($payload['ip_allowlist']) && $payload['ip_allowlist'] !== []
                ? $payload['ip_allowlist']
                : null,
        ])->save();

        return $key;
    }
}
```

- [ ] **Step 4: 编写 ApiKeyUsageLogService**

```php
<?php

declare(strict_types=1);

namespace App\Services\OpenApi;

use App\Models\ApiKeyUsageLog;

class ApiKeyUsageLogService
{
    public function record(int $apiKeyId, int $userId, string $method, string $path, int $statusCode, string $ip, int $durationMs): void
    {
        ApiKeyUsageLog::query()->create([
            'api_key_id' => $apiKeyId,
            'user_id' => $userId,
            'method' => $method,
            'path' => Str::limit($path, 250),
            'status_code' => $statusCode,
            'ip' => $ip,
            'duration_ms' => $durationMs,
            'created_at' => now(),
        ]);
    }
}
```

> 注意：`ApiKeyUsageLog` 模型需同步创建（fillable 对齐迁移字段），或直接使用 `DB::table()` 写入。若创建模型，放 `backend/app/Models/ApiKeyUsageLog.php`。

- [ ] **Step 5: 后端测试**

Run: `cd backend && php -l app/Models/ApiKey.php && php -l app/Services/OpenApi/ApiKeyService.php && php -l app/Services/OpenApi/OpenApiConfig.php`
Expected: 无语法错误。

- [ ] **Step 6: 提交**

```bash
git add backend/app/Models/ApiKey.php backend/app/Models/ApiKeyUsageLog.php backend/app/Services/OpenApi/
git commit -m "feat: open api 密钥模型与生成/校验服务"
```

---

### Task 3: api.key 中间件

**Files:**

- Create: `backend/app/Http/Middleware/AuthenticateApiKey.php`

- [ ] **Step 1: 编写中间件**

```php
<?php

declare(strict_types=1);

namespace App\Http\Middleware;

use App\Models\User;
use App\Services\OpenApi\ApiKeyService;
use App\Services\OpenApi\ApiKeyUsageLogService;
use App\Services\OpenApi\OpenApiConfig;
use App\Support\ApiResponseBuilder;
use Closure;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Log;
use Symfony\Component\HttpFoundation\Response;

class AuthenticateApiKey
{
    public function __construct(
        private readonly ApiKeyService $keys,
        private readonly ApiKeyUsageLogService $logs,
        private readonly OpenApiConfig $config,
    ) {}

    /**
     * @param  array{0?: string, 1?: string}  $required  [domain, level]，如 ['services', 'write']
     */
    public function handle(Request $request, Closure $next, string $domain = '', string $level = 'read'): Response
    {
        $startedAt = microtime(true);
        $statusCode = 200;
        $ip = (string) $request->ip();

        if (! $this->config->enabled()) {
            $statusCode = 403;
            return ApiResponseBuilder::error(40300, '开放接口已关闭');
        }

        try {
            $secret = $this->extractBearerToken($request);
            $key = $this->keys->resolve($secret);
            $this->keys->assertScope($key, $domain, $level);
            $this->keys->assertIpAllowed($key, $ip);

            $user = User::query()->find((int) $key->user_id);
            if (! $user || (int) $user->status !== 1) {
                throw new \App\Exceptions\BusinessException('密钥所属账号不可用', 40300);
            }

            $request->attributes->set('api_key', $key);
            $request->attributes->set('api_key_user', $user);
            $request->setUserResolver(fn () => $user);

            $this->keys->touchLastUsed($key);

            $response = $next($request);
            $statusCode = $response->getStatusCode();
            return $response;
        } catch (\App\Exceptions\BusinessException $exception) {
            $statusCode = $exception->getCode() === 40100 ? 401 : 403;
            return ApiResponseBuilder::error($exception->getCode(), $exception->getMessage());
        } finally {
            try {
                $key = $request->attributes->get('api_key');
                if ($key) {
                    $this->logs->record(
                        (int) $key->id,
                        (int) $key->user_id,
                        (string) $request->method(),
                        (string) $request->path(),
                        $statusCode,
                        $ip,
                        (int) (microtime(true) - $startedAt) * 1000,
                    );
                }
            } catch (\Throwable $logException) {
                Log::warning('[open-api] 审计写入失败', ['error' => $logException->getMessage()]);
            }
        }
    }

    private function extractBearerToken(Request $request): string
    {
        $header = (string) $request->header('Authorization', '');
        if (preg_match('/Bearer\s+(.+)/i', $header, $matches)) {
            return trim($matches[1]);
        }
        return '';
    }
}
```

> 注意：确认 `ApiResponseBuilder::error($code, $message)` 的签名（参考 `EnsureClientAuthenticated` 用法）；`BusinessException` 的 `getCode()` 是业务码还是 HTTP 码需核对（若 code 为 42200 类，则直接用）。

- [ ] **Step 2: 注册中间件别名**

在 `backend/bootstrap/app.php`（或项目使用的 Kernel 位置）的 `->withMiddleware(...)` 中注册别名：

```php
$middleware->alias([
    'api.key' => \App\Http\Middleware\AuthenticateApiKey::class,
]);
```

先查看 `backend/bootstrap/app.php` 现有 `alias` 注册方式，保持同样写法。

- [ ] **Step 3: 语法检查**

Run: `cd backend && php -l app/Http/Middleware/AuthenticateApiKey.php`
Expected: 无语法错误。

- [ ] **Step 4: 提交**

```bash
git add backend/app/Http/Middleware/AuthenticateApiKey.php backend/bootstrap/app.php
git commit -m "feat: open api 密钥认证中间件（scope/IP/审计）"
```

---

### Task 4: 用户端密钥 CRUD 接口（/api/v2/client/api-keys*）

**Files:**

- Create: `backend/app/Http/Controllers/Client/V2/ApiKeyController.php`
- Create: `backend/app/Http/Requests/Client/V2/ApiKeyStoreRequest.php`
- Create: `backend/app/Http/Requests/Client/V2/ApiKeyUpdateRequest.php`
- Modify: `backend/routes/v2-client.php`

- [ ] **Step 1: 编写 ApiKeyController**

```php
<?php

declare(strict_types=1);

namespace App\Http\Controllers\Client\V2;

use App\Http\Controllers\Controller;
use App\Http\Requests\Client\V2\ApiKeyStoreRequest;
use App\Http\Requests\Client\V2\ApiKeyUpdateRequest;
use App\Models\ApiKey;
use App\Models\ApiKeyUsageLog;
use App\Services\OpenApi\ApiKeyService;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;

class ApiKeyController extends Controller
{
    public function __construct(private readonly ApiKeyService $keys) {}

    public function index(Request $request): JsonResponse
    {
        $items = ApiKey::query()
            ->where('user_id', (int) $request->user()->id)
            ->orderByDesc('id')
            ->get()
            ->map(fn (ApiKey $key) => $this->present($key));

        return $this->success(['list' => $items]);
    }

    public function store(ApiKeyStoreRequest $request): JsonResponse
    {
        $data = $request->validated();
        [$key, $plain] = $this->keys->createForUser(
            $request->user(),
            (string) $data['name'],
            (array) ($data['scopes'] ?? []),
            ($data['expires_at'] ?? null) !== '' ? (string) $data['expires_at'] : null,
            (array) ($data['ip_allowlist'] ?? []),
        );

        return $this->success([
            'key' => $this->present($key),
            'secret' => $plain,
            'secret_warning' => '密钥仅显示这一次，请立即妥善保存。',
        ], 'API 密钥创建成功');
    }

    public function update(ApiKeyUpdateRequest $request, int $id): JsonResponse
    {
        $key = $this->findOwnedKey($request, $id);
        $updated = $this->keys->update($key, $request->validated());

        return $this->success(['key' => $this->present($updated)], 'API 密钥已更新');
    }

    public function setStatus(Request $request, int $id): JsonResponse
    {
        $data = $request->validate(['status' => ['required', 'in:enabled,disabled']]);
        $key = $this->findOwnedKey($request, $id);
        $key->forceFill(['status' => $data['status']])->save();

        return $this->success(['key' => $this->present($key)], $data['status'] === 'enabled' ? '密钥已启用' : '密钥已停用');
    }

    public function destroy(Request $request, int $id): JsonResponse
    {
        $key = $this->findOwnedKey($request, $id);
        $key->delete();

        return $this->success([], 'API 密钥已删除');
    }

    public function usageLogs(Request $request, int $id): JsonResponse
    {
        $this->findOwnedKey($request, $id);

        $logs = ApiKeyUsageLog::query()
            ->where('api_key_id', $id)
            ->orderByDesc('created_at')
            ->limit(50)
            ->get()
            ->map(fn (ApiKeyUsageLog $log) => [
                'method' => (string) $log->method,
                'path' => (string) $log->path,
                'status_code' => (int) $log->status_code,
                'ip' => (string) $log->ip,
                'duration_ms' => (int) $log->duration_ms,
                'created_at' => $log->created_at?->format('Y-m-d H:i:s'),
            ]);

        return $this->success(['list' => $logs]);
    }

    private function findOwnedKey(Request $request, int $id): ApiKey
    {
        $key = ApiKey::query()
            ->where('user_id', (int) $request->user()->id)
            ->find($id);

        if (! $key) {
            abort(404);
        }

        return $key;
    }

    private function present(ApiKey $key): array
    {
        return [
            'id' => (int) $key->id,
            'name' => (string) $key->name,
            'key_prefix' => (string) $key->key_prefix,
            'secret_last4' => (string) $key->secret_last4,
            'scopes' => is_array($key->scopes) ? $key->scopes : [],
            'expires_at' => $key->expires_at?->format('Y-m-d H:i:s'),
            'ip_allowlist' => is_array($key->ip_allowlist) ? $key->ip_allowlist : [],
            'status' => (string) $key->status,
            'last_used_at' => $key->last_used_at?->format('Y-m-d H:i:s'),
            'created_at' => $key->created_at?->format('Y-m-d H:i:s'),
        ];
    }
}
```

> 核对现有 Controller 基类的 `success()` 签名（参考 `Client\V2\AuthController` 用法）。`ApiKeyUsageLog` 需在 Task 2 创建模型。

- [ ] **Step 2: 编写 FormRequest**

`ApiKeyStoreRequest`（rules）：`name` required string max:64；`scopes` required array；`scopes.*` in:read,write；`expires_at` nullable date；`ip_allowlist` nullable array。

`ApiKeyUpdateRequest`：`name` sometimes string max:64；`scopes` sometimes array；`expires_at` nullable date；`ip_allowlist` nullable array。

- [ ] **Step 3: 注册路由**

在 `backend/routes/v2-client.php` 的 `auth:sanctum + ensure.client` 分组内追加：

```php
Route::get('/api-keys', [ApiKeyController::class, 'index']);
Route::post('/api-keys', [ApiKeyController::class, 'store'])->middleware('throttle:10,1,client-api-key-store');
Route::put('/api-keys/{id}', [ApiKeyController::class, 'update']);
Route::put('/api-keys/{id}/status', [ApiKeyController::class, 'setStatus']);
Route::delete('/api-keys/{id}', [ApiKeyController::class, 'destroy']);
Route::get('/api-keys/{id}/usage-logs', [ApiKeyController::class, 'usageLogs']);
```

- [ ] **Step 4: 语法检查 + 路由确认**

Run: `cd backend && php -l app/Http/Controllers/Client/V2/ApiKeyController.php && php artisan route:list --path=api-keys`
Expected: 无语法错误；列出 6 条 `/api/v2/client/api-keys*` 路由。

- [ ] **Step 5: 提交**

```bash
git add backend/app/Http/Controllers/Client/V2/ApiKeyController.php backend/app/Http/Requests/Client/V2/ backend/routes/v2-client.php
git commit -m "feat: 用户端 API 密钥 CRUD 接口"
```

---

### Task 5: open 网关路由 + 商品/报价接口

**Files:**

- Create: `backend/routes/v2-open.php`
- Create: `backend/app/Http/Controllers/Open/V2/OpenProductController.php`
- Create: `backend/app/Http/Requests/Open/V2/OpenQuoteRequest.php`

- [ ] **Step 1: 编写 v2-open.php 路由**

```php
<?php

use App\Http\Controllers\Open\V2\OpenFinanceController;
use App\Http\Controllers\Open\V2\OpenOrderController;
use App\Http\Controllers\Open\V2\OpenProductController;
use App\Http\Controllers\Open\V2\OpenServiceController;
use Illuminate\Support\Facades\Route;

Route::middleware(['api.key:products,read'])->group(function (): void {
    Route::get('/products', [OpenProductController::class, 'index']);
    Route::get('/products/{product}', [OpenProductController::class, 'show']);
    Route::get('/products/{product}/quotes', [OpenProductController::class, 'quotes']);
});

Route::middleware(['api.key:orders,read'])->group(function (): void {
    Route::get('/orders', [OpenOrderController::class, 'index']);
    Route::get('/orders/{invoice}', [OpenOrderController::class, 'show']);
});

Route::middleware(['api.key:orders,write'])->group(function (): void {
    Route::post('/orders', [OpenOrderController::class, 'store']);
    Route::post('/orders/{invoice}/pay', [OpenOrderController::class, 'payByBalance']);
});

Route::middleware(['api.key:services,read'])->group(function (): void {
    Route::get('/services', [OpenServiceController::class, 'index']);
    Route::get('/services/{service}', [OpenServiceController::class, 'show']);
    Route::get('/services/{service}/renewals', [OpenServiceController::class, 'renewPreview']);
});

Route::middleware(['api.key:services,write'])->group(function (): void {
    Route::post('/services/{service}/power', [OpenServiceController::class, 'power']);
    Route::post('/services/{service}/renew', [OpenServiceController::class, 'renew']);
    Route::post('/services/{service}/reinstall', [OpenServiceController::class, 'reinstall']);
});

Route::middleware(['api.key:finance,read'])->group(function (): void {
    Route::get('/balance', [OpenFinanceController::class, 'balance']);
});

Route::middleware(['api.key'])->group(function (): void {
    Route::get('/keys/self', [OpenServiceController::class, 'selfKey']);
    Route::post('/keys/self/disable', [OpenServiceController::class, 'disableSelfKey']);
});
```

> 注意：`api.key:products,read` 中间件参数传 scope。路由文件需在路由服务注册处（`bootstrap/app.php` 或 `routes/console.php` 同级配置）加载，参考 `v2-client.php` 的加载方式，前缀统一 `/api/v2/open`。

- [ ] **Step 2: 编写 OpenProductController（复用商品目录 + 报价）**

```php
<?php

declare(strict_types=1);

namespace App\Http\Controllers\Open\V2;

use App\Http\Controllers\Controller;
use App\Http\Requests\Open\V2\OpenQuoteRequest;
use App\Models\Product;
use App\Services\Finance\CheckoutService;
use App\Services\Finance\CheckoutSecurityService;
use App\Services\ProductCatalog\ProductCatalogService;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;

class OpenProductController extends Controller
{
    public function __construct(
        private readonly ProductCatalogService $catalog,
        private readonly CheckoutService $checkout,
        private readonly CheckoutSecurityService $security,
    ) {}

    public function index(Request $request): JsonResponse
    {
        // 复用商品目录可售商品查询；按现有管理/站点商品的公开字段返回
        $products = Product::query()
            ->where('status', 1)
            ->where('stock', '>', 0)
            ->orderByDesc('id')
            ->limit(100)
            ->get();

        return $this->success([
            'list' => $products->map(fn (Product $product) => [
                'id' => (int) $product->id,
                'name' => (string) $product->name,
                'product_type' => (string) $product->product_type,
                'price_text' => $this->resolvePriceText($product),
                'stock' => (int) $product->stock,
            ]),
        ]);
    }

    public function show(Product $product): JsonResponse
    {
        abort_unless((int) $product->status === 1, 404);

        return $this->success([
            'id' => (int) $product->id,
            'name' => (string) $product->name,
            'product_type' => (string) $product->product_type,
            'price_text' => $this->resolvePriceText($product),
            'billing_cycles' => $this->resolveBillingCycles($product),
            'stock' => (int) $product->stock,
        ]);
    }

    public function quotes(OpenQuoteRequest $request, Product $product): JsonResponse
    {
        $data = $request->validated();
        $quote = $this->checkout->quote(
            $product,
            (string) $data['billing_cycle'],
            (array) ($data['config'] ?? []),
            max((int) ($data['quantity'] ?? 1), 1),
        );
        $token = $this->security->issueQuoteToken(
            (int) $product->id,
            (string) $data['billing_cycle'],
            (array) ($data['config'] ?? []),
            $quote,
        );

        return $this->success([
            'product_id' => (int) $product->id,
            'billing_cycle' => (string) $data['billing_cycle'],
            'total_amount' => $quote['total_amount'] ?? '0.00',
            'quote_token' => (string) ($token['quote_token'] ?? ''),
        ]);
    }

    private function resolvePriceText(Product $product): string
    {
        // 简单返回起售价文本；如商品有起始价格字段则读取，否则占位
        return (string) ($product->price_text ?? '');
    }

    private function resolveBillingCycles(Product $product): array
    {
        // 返回商品支持的计费周期；如商品无该字段则返回 ['monthly']
        return ['monthly'];
    }
}
```

> 重要：`CheckoutService::quote` 的真实签名与返回字段、`Product` 可售判定字段、价格文本/计费周期来源，**必须先读取 `CheckoutService::quote` 与 `Product` 模型/资源实现再对齐**。若 `quote` 需要商品配置校验，参考 `SiteProductQuoteService::quoteForUser` 的调用方式。`resolvePriceText`/`resolveBillingCycles` 需按真实字段实现，不要留占位逻辑。

- [ ] **Step 3: 编写 OpenQuoteRequest**

rules：`billing_cycle` required string；`config` nullable array；`quantity` nullable int min:1。

- [ ] **Step 4: 验证路由与语法**

Run: `cd backend && php -l routes/v2-open.php && php -l app/Http/Controllers/Open/V2/OpenProductController.php && php artisan route:list --path=open`
Expected: 无语法错误；列出 `/api/v2/open/*` 路由。

- [ ] **Step 5: 提交**

```bash
git add backend/routes/v2-open.php backend/app/Http/Controllers/Open/V2/OpenProductController.php backend/app/Http/Requests/Open/V2/OpenQuoteRequest.php
git commit -m "feat: open api 商品目录与报价接口"
```

---

### Task 6: open 订单/服务/余额接口

**Files:**

- Create: `backend/app/Http/Controllers/Open/V2/OpenOrderController.php`
- Create: `backend/app/Http/Controllers/Open/V2/OpenServiceController.php`
- Create: `backend/app/Http/Controllers/Open/V2/OpenFinanceController.php`
- Create: `backend/app/Http/Requests/Open/V2/OpenOrderStoreRequest.php`

- [ ] **Step 1: 编写 OpenOrderController**

```php
<?php

declare(strict_types=1);

namespace App\Http\Controllers\Open\V2;

use App\Http\Controllers\Controller;
use App\Http\Requests\Open\V2\OpenOrderStoreRequest;
use App\Models\Invoice;
use App\Models\User;
use App\Services\Finance\CheckoutService;
use App\Services\Finance\PaymentService;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;

class OpenOrderController extends Controller
{
    public function __construct(
        private readonly CheckoutService $checkout,
        private readonly PaymentService $payments,
    ) {}

    public function store(OpenOrderStoreRequest $request): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $data = $request->validated();

        $invoice = $this->checkout->create((int) $user->id, [
            'product_id' => (int) $data['product_id'],
            'billing_cycle' => (string) $data['billing_cycle'],
            'quantity' => max((int) ($data['quantity'] ?? 1), 1),
            'config' => (array) ($data['config'] ?? []),
            'quote_token' => (string) $data['quote_token'],
        ], [
            'idempotency_key' => (string) $data['idempotency_key'],
            'trace_id' => 'open:'.$request->attributes->get('api_key')->key_prefix,
            'ip_address' => (string) $request->ip(),
        ]);

        return $this->success($this->presentInvoice($invoice), '下单成功');
    }

    public function payByBalance(Request $request, int $invoice): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $target = $this->findOwnedInvoice($user, $invoice);

        $this->payments->payByBalance($target, $user, ['trace_id' => 'open:pay:'.$target->invoice_no]);

        return $this->success($this->presentInvoice($target->fresh()), '支付成功');
    }

    public function index(Request $request): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $items = Invoice::query()
            ->where('user_id', (int) $user->id)
            ->orderByDesc('id')
            ->limit(50)
            ->get()
            ->map(fn (Invoice $invoice) => $this->presentInvoice($invoice));

        return $this->success(['list' => $items]);
    }

    public function show(Request $request, int $invoice): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        return $this->success($this->presentInvoice($this->findOwnedInvoice($user, $invoice)));
    }

    private function findOwnedInvoice(User $user, int $id): Invoice
    {
        $invoice = Invoice::query()
            ->where('user_id', (int) $user->id)
            ->find($id);

        if (! $invoice) {
            abort(404);
        }

        return $invoice;
    }

    private function presentInvoice(Invoice $invoice): array
    {
        return [
            'id' => (int) $invoice->id,
            'invoice_no' => (string) $invoice->invoice_no,
            'product_name' => (string) ($invoice->product_spec_snapshot ?? ''),
            'amount' => (string) $invoice->amount,
            'payable_amount' => (string) ($invoice->payable_amount ?? $invoice->amount),
            'status' => (string) ($invoice->status ?? ''),
            'paid_at' => $invoice->paid_at?->format('Y-m-d H:i:s'),
            'created_at' => $invoice->created_at?->format('Y-m-d H:i:s'),
        ];
    }
}
```

> 核对 `Invoice` 模型字段（`payable_amount`/`status` 取值）、`PaymentService::payByBalance` 的返回类型与是否可能抛错（如余额不足抛出 BusinessException → 由统一异常处理转 42200）。参考 `LocalProductSimulateCommand::doNew()` 的调用链。

- [ ] **Step 2: 编写 OpenServiceController**

```php
<?php

declare(strict_types=1);

namespace App\Http\Controllers\Open\V2;

use App\Http\Controllers\Controller;
use App\Models\Service;
use App\Models\User;
use App\Services\ClientServiceConsole\ServiceDetailService;
use App\Services\ClientServiceConsole\ServicePowerService;
use App\Services\Provisioning\ServiceRenewService;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;

class OpenServiceController extends Controller
{
    public function __construct(
        private readonly ServiceDetailService $detail,
        private readonly ServicePowerService $power,
        private readonly ServiceRenewService $renew,
    ) {}

    public function index(Request $request): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $items = Service::query()
            ->where('user_id', (int) $user->id)
            ->orderByDesc('id')
            ->limit(100)
            ->get()
            ->map(fn (Service $service) => $this->present($service));

        return $this->success(['list' => $items]);
    }

    public function show(Request $request, int $service): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $detail = $this->detail->detailForUser($user, $service);

        return $this->success($detail);
    }

    public function power(Request $request, int $service): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $action = (string) $request->validate(['action' => ['required', 'in:on,off,reboot,hard_off,hard_reboot']])['action'];
        $result = $this->power->powerActionForUser($user, $service, $action, $this->context($request));

        return $this->success($result, '操作已提交');
    }

    public function renew(Request $request, int $service): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $cycle = (string) $request->validate(['billing_cycle' => ['required', 'string']])['billing_cycle'];
        $invoice = $this->renew->createRenewInvoiceForUser($user, $service, $cycle, 0, $this->context($request));

        return $this->success([
            'invoice_id' => (int) $invoice->id,
            'invoice_no' => (string) $invoice->invoice_no,
            'amount' => (string) $invoice->amount,
        ], '续费账单已创建');
    }

    public function reinstall(Request $request, int $service): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        $data = $request->validate(['os_template_id' => ['nullable', 'integer']]);
        $result = $this->power->reinstallForUser($user, $service, $data, $this->context($request));

        return $this->success($result, '重装已提交');
    }

    public function renewPreview(Request $request, int $service): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');
        // 复用用户端续费预览逻辑（ServiceConsoleController::renewPreview 调用的 service）
        return $this->success($this->detail->renewPreviewForUser($user, $service));
    }

    public function selfKey(Request $request): JsonResponse
    {
        $key = $request->attributes->get('api_key');
        return $this->success([
            'key_prefix' => (string) $key->key_prefix,
            'scopes' => is_array($key->scopes) ? $key->scopes : [],
            'expires_at' => $key->expires_at?->format('Y-m-d H:i:s'),
            'last_used_at' => $key->last_used_at?->format('Y-m-d H:i:s'),
        ]);
    }

    public function disableSelfKey(Request $request): JsonResponse
    {
        $key = $request->attributes->get('api_key');
        $key->forceFill(['status' => 'disabled'])->save();

        return $this->success([], '密钥已停用');
    }

    private function context(Request $request): array
    {
        return [
            'trace_id' => 'open:'.$request->attributes->get('api_key')->key_prefix,
            'ip_address' => (string) $request->ip(),
            'actor' => 'open_api',
        ];
    }

    private function present(Service $service): array
    {
        return [
            'id' => (int) $service->id,
            'name' => (string) $service->name,
            'product_name' => (string) ($service->product?->name ?? ''),
            'status' => (string) $service->status,
            'expires_at' => $service->expires_at?->format('Y-m-d H:i:s'),
            'created_at' => $service->created_at?->format('Y-m-d H:i:s'),
        ];
    }
}
```

> 关键核对：`ServiceDetailService` 的服务详情方法名（`detailForUser` 还是别的）、`renewPreviewForUser` 是否存在。**必须读取 `backend/app/Services/ClientServiceConsole/ServiceDetailService.php` 与 `ServiceConsoleController.php` 的真实方法名后对齐**，不要照抄假设名。续费接口若 `createRenewInvoiceForUser` 只建单不支付，需在 Open 控制器补一步 `PaymentService::payByBalance`（与设计一致"创建续费账单并余额支付"）。

- [ ] **Step 3: 编写 OpenFinanceController**

```php
<?php

declare(strict_types=1);

namespace App\Http\Controllers\Open\V2;

use App\Http\Controllers\Controller;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;

class OpenFinanceController extends Controller
{
    public function balance(Request $request): JsonResponse
    {
        $user = $request->attributes->get('api_key_user');

        return $this->success([
            'balance' => (string) $user->balance,
        ]);
    }
}
```

- [ ] **Step 4: 编写 OpenOrderStoreRequest**

rules：`product_id` required integer；`billing_cycle` required string；`quote_token` required string；`idempotency_key` required string max:64；`quantity` nullable integer min:1；`config` nullable array。

- [ ] **Step 5: 语法检查 + 路由确认**

Run: `cd backend && php artisan route:list --path=open`
Expected: 列出全部 `/api/v2/open/*` 路由，无语法错误。

- [ ] **Step 6: 提交**

```bash
git add backend/app/Http/Controllers/Open/V2/ backend/app/Http/Requests/Open/V2/
git commit -m "feat: open api 订单/服务/余额接口"
```

---

### Task 7: 后端测试（密钥 + open 端到端）

**Files:**

- Create: `backend/tests/Feature/OpenApi/ApiKeyTest.php`
- Create: `backend/tests/Feature/OpenApi/OpenApiFlowTest.php`

- [ ] **Step 1: 编写 ApiKeyTest（覆盖创建/哈希/前置条件/scope/IP/过期）**

参考现有 Feature 测试风格（`Sanctum::actingAs` + `$this->postJson`）。核心用例：

- 创建成功返回明文 secret 且仅返回一次；`secret_hash` 不等于明文；`key_prefix` 以 `tura_` 开头
- 未绑手机 + 开启 require_phone → 403
- 未实名 + 开启 require_verified → 403
- 超出 max_keys_per_user → 422
- scope 校验：`api.key` 中间件对缺失 scope 返回 403
- IP 白名单：不在白名单返回 403

- [ ] **Step 2: 编写 OpenApiFlowTest（端到端：报价→下单→余额支付→服务查询）**

- 创建密钥（orders:write + services:read + products:read）
- 调用 `GET /api/v2/open/products` 成功
- 调用 `GET /api/v2/open/products/{id}/quotes` 获得 quote_token
- 调用 `POST /api/v2/open/orders`（带 idempotency_key）创建 Invoice
- 相同 idempotency_key 重复提交返回同一 Invoice（幂等）
- 余额充足时 `POST /api/v2/open/orders/{id}/pay` 支付成功
- 余额不足时支付抛 422
- 用错误密钥 / 已禁用密钥 / 已过期密钥请求 → 401
- 无 services:write 权限的密钥调用 power → 403

- [ ] **Step 3: 运行测试**

Run: `cd backend && php artisan test tests/Feature/OpenApi/`
Expected: 全部通过。

- [ ] **Step 4: 提交**

```bash
git add backend/tests/Feature/OpenApi/
git commit -m "test: open api 密钥与端到端流程测试"
```

---

### Task 8: 用户端「API 密钥」页面（frontend-user-v4-console）

**Files:**

- Create: `frontend-user-v4-console/src/api/client/apiKeys.ts`
- Create: `frontend-user-v4-console/src/pages/client/api-keys/index.vue`
- Modify: `frontend-user-v4-console/src/router/modules/client.ts`

- [ ] **Step 1: 编写 API 封装**

```ts
import http from "@/api/client/http"; // 按现有 api 封装惯例（查看 src/api/ 现有文件确认 import 路径）

export interface ApiKeyItem {
  id: number;
  name: string;
  key_prefix: string;
  secret_last4: string;
  scopes: Record<string, "read" | "write">;
  expires_at?: string;
  ip_allowlist?: string[];
  status: "enabled" | "disabled";
  last_used_at?: string;
  created_at?: string;
}

export function fetchApiKeys() {
  return http.get("/client/api-keys");
}

export function createApiKey(payload: {
  name: string;
  scopes: Record<string, "read" | "write">;
  expires_at?: string;
  ip_allowlist?: string[];
}) {
  return http.post("/client/api-keys", payload);
}

export function updateApiKey(
  id: number,
  payload: Partial<{
    name: string;
    scopes: Record<string, "read" | "write">;
    expires_at?: string;
    ip_allowlist?: string[];
  }>,
) {
  return http.put(`/client/api-keys/${id}`, payload);
}

export function setApiKeyStatus(id: number, status: "enabled" | "disabled") {
  return http.put(`/client/api-keys/${id}/status`, { status });
}

export function deleteApiKey(id: number) {
  return http.delete(`/client/api-keys/${id}`);
}

export function fetchApiKeyUsageLogs(id: number) {
  return http.get(`/client/api-keys/${id}/usage-logs`);
}
```

> 先查看 `frontend-user-v4-console/src/api/` 下现有模块的 http 导入方式与响应解包方式（`res.data` 结构），对齐后再定稿。若现有 api 文件有统一响应类型，一并复用。

- [ ] **Step 2: 编写页面**

页面结构（对齐控制台页面风格，参考 `src/pages/client/` 现有列表页）：

- 列表：名称、`key_prefix` + `secret_last4`、权限标签（按 scopes 渲染）、状态 Tag、过期时间、最后使用、操作按钮
- 「创建密钥」按钮 → 弹窗：名称输入 + 业务域×读写勾选矩阵（4 个域 × read/write）+ 可选过期时间（t-date-picker）+ 可选 IP 白名单（tags 输入）
- 创建成功 → 高亮展示 secret 一次 + 复制按钮 + 警示文案
- 操作：停用/启用、删除（确认）、查看使用日志（抽屉）
- 前置条件被拒（403）时展示后端 message，可跳转实名/绑定手机

- [ ] **Step 3: 注册路由与菜单**

在 `src/router/modules/client.ts` 的 `/client` 布局 children 中追加：

```ts
{
  path: '/client/api-keys',
  name: 'ClientApiKeys',
  component: () => import('@/pages/client/api-keys/index.vue'),
  meta: { title: title('API 密钥', 'API Keys') },
},
```

同时按该文件现有侧栏菜单的注册方式（`meta.menu` 或菜单分组数组）加入「API 密钥」菜单项（图标可用 `KeyIcon` 或 `LockOnIcon`）。

- [ ] **Step 4: 构建验证**

Run: `cd frontend-user-v4-console && npx vue-tsc --noEmit -p tsconfig.json && npx vite build`
Expected: 类型检查与构建通过。

- [ ] **Step 5: 提交**

```bash
git add frontend-user-v4-console/src/api/client/apiKeys.ts frontend-user-v4-console/src/pages/client/api-keys/ frontend-user-v4-console/src/router/modules/client.ts
git commit -m "feat: 用户端 API 密钥管理页"
```

---

### Task 9: 管理端（frontend-admin-v3）：开放接口配置 + 密钥管理 + 权限点

**Files（后端）:**

- Modify: `backend/app/Support/AdminPermissions.php`
- Modify: `backend/app/Services/Admin/Permission/PermissionCatalogService.php`（如存在，登记新权限）
- Create: `backend/app/Http/Controllers/Admin/V2/OpenApiAdminController.php`
- Modify: `backend/routes/v2-admin.php`

**Files（前端）:**

- Create: `frontend-admin-v3/src/pages/settings/open-api/index.vue`（或复用 `settings/index.vue` + settingsTab）
- Create: `frontend-admin-v3/src/pages/open-api/keys.vue`
- Modify: `frontend-admin-v3/src/api/admin/openApi.ts`
- Modify: `frontend-admin-v3/src/router/modules/admin/system.ts`（或对应菜单文件）
- Modify: `frontend-admin-v3/src/store/modules/permission.ts`（`ADMIN_MENU_GROUPS` 注册菜单）
- Modify: `frontend-admin-v3/src/locales/lang/zh_CN.json`、`en_US.json`

- [ ] **Step 1: 后端权限点**

`AdminPermissions.php` 新增常量 `OPEN_API_VIEW = 'open_api.view'`、`OPEN_API_MANAGE = 'open_api.manage'`；`adminDefaultPermissions()` 加入 `OPEN_API_MANAGE`；`impliedPermissions` 加入 `OPEN_API_MANAGE => [OPEN_API_VIEW]`。参照现有 `AGENT_DISCOUNT_MANAGE` 的写法。登记到权限目录（`PermissionCatalogService`）。

- [ ] **Step 2: 后端管理接口（OpenApiAdminController）**

- `GET /api/v2/admin/open-api/config` → 返回 open_api 配置
- `PUT /api/v2/admin/open-api/config` → 保存配置（复用 `Setting::setValues('open_api', ...)`）
- `GET /api/v2/admin/open-api/keys` → 分页列出全部密钥（带用户昵称）
- `PUT /api/v2/admin/open-api/keys/{id}/status` → 禁用/启用
- `DELETE /api/v2/admin/open-api/keys/{id}` → 删除
- `GET /api/v2/admin/open-api/keys/{id}/usage-logs` → 调用审计

权限中间件：`permission:open_api.view`（查看）、`permission:open_api.manage`（写操作）。参照现有 admin 控制器权限写法。

- [ ] **Step 3: 管理端配置页**

复用 `frontend-admin-v3/src/pages/settings/index.vue` 的 settingsTab 模式：新增 `settingsTab: 'open_api'` 的路由（如 `/admin/open-api/settings`），并在 settings 页注册该 tab 的配置项定义（总开关、前置条件两个 switch、数量上限 number、限流 number）。

> 先读取 `frontend-admin-v3/src/pages/settings/index.vue`，确认 tab 定义与配置 schema 的组织方式（参考 `automation`、`log_archive` 两个已有 tab），按相同方式新增 `open_api` tab。

- [ ] **Step 4: 管理端密钥管理页**

列表页（对齐管理端列表页风格）：用户、密钥前缀、权限标签、状态、过期、最后使用、操作（禁用/启用/删除/日志抽屉）。

- [ ] **Step 5: 注册路由与菜单**

路由（system.ts 或独立）：`/admin/open-api/keys`（permission: `open_api.view`）。菜单：在 `ADMIN_MENU_GROUPS` 注册（如「系统设置」组下新增子菜单，或独立组）。中英文 locale 文案同步。

- [ ] **Step 6: 构建验证**

Run: `cd frontend-admin-v3 && npx vue-tsc --noEmit -p tsconfig.json && npx vite build`
Expected: 类型检查与构建通过。

- [ ] **Step 7: 提交**

```bash
git add backend/app/Support/AdminPermissions.php backend/app/Services/Admin/Permission/ backend/app/Http/Controllers/Admin/V2/OpenApiAdminController.php backend/routes/v2-admin.php frontend-admin-v3/src/api/admin/openApi.ts frontend-admin-v3/src/pages/settings/ frontend-admin-v3/src/pages/open-api/ frontend-admin-v3/src/router/modules/admin/system.ts frontend-admin-v3/src/store/modules/permission.ts frontend-admin-v3/src/locales/lang/zh_CN.json frontend-admin-v3/src/locales/lang/en_US.json
git commit -m "feat: 管理端开放接口配置与密钥管理"
```

---

### Task 10: 全量验证与提交推送

- [ ] **Step 1: 后端全量测试**

Run: `cd backend && php artisan test`
Expected: 原有测试 + 新增 OpenApi 测试全部通过。

- [ ] **Step 2: 前端全量构建**

Run: `cd frontend-user-v4-console && npx vue-tsc --noEmit -p tsconfig.json && npx vite build`
Run: `cd frontend-admin-v3 && npx vue-tsc --noEmit -p tsconfig.json && npx vite build`
Expected: 均通过。

- [ ] **Step 3: 提交推送**

```bash
git add -A && git commit -m "feat: 开放 API（Open API v2）全量落地" && git push origin main
```

> 若存在运行期媒体/静态文件（`backend/public/media/*`、`backend/storage/*`）出现在 git status，**不要** `git add -A` 纳入，仅提交本次相关文件。

---

## Self-Review 对照

| 设计文档章节                                  | 对应任务                                                            |
| --------------------------------------------- | ------------------------------------------------------------------- |
| 4. 数据模型（api_keys / usage_logs / 配置项） | Task 1、Task 2                                                      |
| 5. 权限模型（业务域×读写、write 蕴含 read）   | Task 2（normalizeScopes/assertScope）、Task 3 中间件                |
| 6. 接口清单（商品/订单/服务/财务/密钥 self）  | Task 5、Task 6                                                      |
| 7. 下单支付数据流（quote→order→pay→轮询）     | Task 6 OpenOrderController                                          |
| 8. 用户端页面                                 | Task 8                                                              |
| 9. 管理端配置/密钥管理/权限点                 | Task 9                                                              |
| 11. 测试策略                                  | Task 7、Task 10                                                     |
| 安全（哈希/IP/过期/限流/审计）                | Task 2（hashSecret/assertIpAllowed）、Task 3（审计）、路由 throttle |

**已知待核对的真实实现点（子代理执行时优先读取再对齐，不得照抄假设名）：**

1. `CheckoutService::quote` 签名与返回字段、`Product` 可售判定字段、价格/计费周期来源
2. `ServiceDetailService` 的服务详情方法与续费预览方法真实名
3. `ApiResponseBuilder::error` 签名、`Controller::success` 签名
4. `BusinessException` 的 code 语义（HTTP vs 业务码）
5. `bootstrap/app.php` 中间件别名与 v2 路由文件加载方式
6. 用户端 `src/api/` 的 http 封装与响应解包方式；管理端 settings tab 的组织方式
7. 用户端控制台菜单注册方式（`client.ts` meta.menu 或分组数组）
